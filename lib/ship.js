const congigureShip = (game) => {
  const shipOptions = {}

  // position ship on canvas

};

class Ship extends createjs.Bitmap{
  constructor(options) {
    super(options);
    this.vel = new Vector();
  }



}
